import {SafeAreaView, ScrollView, StatusBar, Text, View} from 'react-native';
import Auth from './screens/Auth';

const App = () => {
  return (
    <View>
        <Auth />
      </View>
  );
};

export default App;
